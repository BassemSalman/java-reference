package OOP;

public class Polymorphism {
    /* Multi - Forms
     * Allows object to  
     * 
     * how? inheritence then method overriding
     * 
     */

    /*
     * public class Animal {
     *  void makeSound(){ sout("Animal Sound");}
     * }
     * public class Dog extends Animal{
     *  void makeSound(){ sout("Dog Sound");} // overriding
     *  void makeSound(String s){ sout(s); } // overloading
     * }
     * 
     * overloading => same name and type but different parameters and implementation
     * overriding => same name (could be different type) but different implementation
     * 
     * for(Animal a : animals) {
     *  a.makeSound(); // if a was actually a dog => Dog Sound
     * }
     * 
     * -- Binding --
     * Runtime Polymorphism / Dynamic : Appropriate Method is chosen at runtime ( actual type dependant )
     * Static Polymorphism: Method depends on Declared Type whatever actual type was - compile time
     */



}
